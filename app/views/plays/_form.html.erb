<%= form_for(@play, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @play.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@play.errors.count, "error") %> prohibited this play from being saved:</h4>

      <ul>
      <% @play.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :team, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :team, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :play_type, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :play_type, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :direction, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :direction, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :yards, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :yards, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
